using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class movauto : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject Machinna;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
